import { ProductCategoryWithChildren } from '@/lib/types/global';

export default function SideMenuCategoryItem({
  product_category,
}: {
  product_category: ProductCategoryWithChildren;
}) {
  return <div>SideMenuCategoryItem</div>;
}
