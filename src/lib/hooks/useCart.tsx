const useCart = () => {
  const handleAddCart = async ({ variantId }: { variantId: string }) => {
    if (!variantId) return null;

    
    

  };

  return {
    cart: null,
  };
};

export default useCart;
